import React from 'react';

const Payment = () => {
  return (
    <div
      className="container"
      style={{ margin: 'auto', background: 'yellow', padding: '2rem' }}
    >
      Payment
    </div>
  );
};

export default Payment;
